<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="univalipoo.leoaula.com.br.a04_calculadoraarea">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".passo1_circulo" />
        <activity android:name=".passo1_quadrado" />
        <activity android:name=".passo1_triangulo" />
        <activity android:name=".resultado_circulo" />
        <activity android:name=".resultado_quadrado" />
        <activity android:name=".resultado_triangulo"></activity>
    </application>

</manifest>